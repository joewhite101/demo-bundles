version: entando.org/v1alpha1
code: keycloak_bundle
description: This bundle includes a single widget that can be used to redirect the user to the SME keycloak instance for authentication via 2FA. A page model and styling should be installed separately to use this widget

components:

  widgets:
    - widgets/keycloak-redirect.yaml
