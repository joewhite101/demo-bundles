code: keycloak_bundle
description: This bundle includes a single widget that can be used to redirect the user to the keycloak instance for authentication via 2FA.

components:

  widgets:
    - widgets/keycloak-redirect.yaml
